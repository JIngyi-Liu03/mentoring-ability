# 版本隔离与 002（AI 解读）实施说明

> 适用对象：本项目维护者
> 当前日期：2026-08-06
> 目的：在不干扰线上 001 的前提下，独立开发、测试 002（AI 解读），通过后再替换上线。

---

## 一、目标

| 目标 | 说明 |
|---|---|
| **001 保持线上** | 用户端 3001 / 管理端 3002 照常服务，代码、端口、进程**完全不动** |
| **002 隔离开发** | AI 解读功能只在 `v002` 分支写，本地用 5001/5002 测试，不碰 3001 |
| **测试通过再上线** | 002 验证 OK 后，才合并回 `main`、部署、替换 001 |

一句话：**001 是现在线上跑的；002 是一份从 001 复制出来、随便改的草稿；草稿合格了才印成新版上架。**

---

## 二、三个隔离（为什么互不影响）

1. **分支隔离（代码层）**——`main`(001) 与 `v002`(002) 是 git 里两条独立时间线，同一文件夹靠 `git checkout` 切换。改 `v002` 不会动 `main`。
2. **端口隔离（运行层）**——线上 3001/3002，002 本地测试用 5001/5002/5003，互不重叠，是独立进程。
3. **部署隔离（发布层）**——服务器只 `git pull` 它跟踪的 `main` 分支；`v002` 的提交推到 GitHub 后，服务器**看不见**，除非合并进 `main`。

---

## 三、位置地图：每个"位置 / 名词"指什么

| 名称 | 类型 | 当前值 | 它指什么 |
|---|---|---|---|
| **main** | 分支 | `7dcdd31` | **线上 001 的源代码**。服务器部署的就是它，是 001 的"准绳"，全程不改动 |
| **v002** | 分支 | `7dcdd31`（暂时与 main 相同） | **002 的开发线**。从 main 长出来，已继承 001 全部代码；AI 解读代码只写在这里 |
| **v001** | 标签 | `4d706cc` | 原计划作为"001 发布锚点"的标签。**当前指向与 main 不一致，待对齐到 `7dcdd31`**（见第六节） |
| **7dcdd31** | 提交 | — | `main` 与 `v002` 当前共同指向的提交，即"当前 001 代码"的精确快照 |
| **3001 / 3002** | 端口 | 线上占用 | 001 的用户端 / 管理端端口（服务器上跑） |
| **5001 / 5002 / 5003** | 端口 | 本地空闲 | 002 本地测试端口：后端 5001、前端 5002、管理端 5003。**只存在于你本地，不上生产** |
| **dev:002** | npm 脚本 | `package.json` | 一键起 002 测试环境（`cross-env PORT=5001 ADMIN_PORT=5003 … vite --port 5002`） |
| **GitHub 仓库** | 远程 | `origin` | 代码中转站，按分支存：`main`=001、`v002`=002，互不影响 |
| **服务器（部署机）** | 独立机器 | 远程 | 跑线上 3001 的机器；部署脚本只 `git pull`（跟踪 main），不看 v002 |
| **本地机器** | 你的电脑 | 本机 | 你编辑代码、本地预览 5002 的地方；与服务器是两台机器 |

> 易混点：**"一个文件夹"不是问题**。这个文件夹是本地副本，靠分支切换呈现不同版本；线上 3001 在另一台机器上，不读这个文件夹。

---

## 四、目录与文件位置

```
mentor-ability001/                ← 项目根（本地副本，当前在 v002 分支）
├── src/                          ← 前端源码（Vue）
│   ├── router/index.js           ← 路由；002 将在此加 /report/:id（deep-report）
│   ├── views/ResultView.vue      ← 基础报告页；002 将在此加「AI解读」按钮
│   ├── views/report/             ← 002 新增：AiReportView.vue（深度报告页）
│   ├── services/                 ← 002 新增：interpretation.js（报告纯函数）
│   └── components/coach/         ← 002 新增：CoachChat.vue（Coze 对话教练）
├── server/                       ← 后端（Node）；002 不新增接口，复用 001 的 API
│   └── src/index.js              ← 端口取自 process.env.PORT || 3001（已支持环境变量）
├── shared/                       ← 前后端共享的数据契约（维度/题目/等级/建议）
├── deploy/                       ← 部署脚本（setup-server.sh / update.sh）
│   └── setup-server.sh           ← 服务器 git pull / clone（默认 main）
├── vite.config.js                ← 前端代理目标取自 VITE_API_TARGET || 3001（已支持环境变量）
├── package.json                  ← 已含 dev:002 脚本；cross-env 已装
└── VERSION-PLAN.md               ← 本说明文件
```

**002 功能落地后将新增/改动的文件（全部只在 v002）：**
- 新增 `src/services/interpretation.js` —— 生成深度报告的纯函数（不接 AI）
- 新增 `src/views/report/AiReportView.vue` —— 深度报告编排页
- 新增 `src/components/coach/CoachChat.vue` —— Coze SDK 内联嵌入封装
- 改动 `src/router/index.js` —— 加 `/report/:id` 路由
- 改动 `src/views/ResultView.vue` —— 加「AI解读」按钮（原「专业导师解读」先注释隐藏）

---

## 五、操作流程

### 1. 开发 002（在 v002 分支）
```powershell
git checkout v002        # 确认在 002 线
# 写代码（仅新增/改动上述文件）
```

### 2. 本地测试 002（不碰 3001）
```powershell
npm run dev:002
# 打开 http://124.221.158.216:5002 即 002 测试环境
#   后端 5001 / 前端 5002 / 代理指向 5001
```
> 若要在服务器做"类生产"隔离测试：另建实例、用 `PORT=3003` 等，与 3001 不冲突（按需再加）。

### 3. 上线 002（替换 001，需你批准才执行）
```powershell
git checkout main
git merge v002           # 把 002 合并进 001 线
git push origin main     # 推到 main
```
服务器：`git pull` → `npm run build:all` → `systemctl restart mentor-ability`
→ 线上 3001 即变为 002。随后 `git tag v002-release`。测试实例（5001/5002）可下线。

### 4. 逃生门（002 改毁了，随时跳出，绝不伤 001）
| 逃生门 | 命令 | 效果 |
|---|---|---|
| 切回 001 | `git checkout main` | 文件夹变回纯 001，v002 工作放下 |
| 清空 002 重来 | 在 v002：`git reset --hard 7dcdd31` | 抹掉 v002 全部改动，从 001 原样重启 |
| 删掉 002 | `git branch -D v002` 再 `git checkout -b v002` | 扔掉整条 002 线，重新拉 |

> 三道门都不会动 `main`(7dcdd31)，线上 001 永远可取用。

---

## 六、当前进度与待办

**已完成**
- [x] 在提交 `7dcdd31` 打 `v001` 标签（注：当前指向 `4d706cc`，待对齐）
- [x] 从 001 切出 `v002` 开发分支
- [x] `package.json` 增加 `dev:002` 一键脚本（5001/5002/5003）
- [x] 安装 `cross-env` 开发依赖（保证 Windows 环境变量写法兼容）

**待处理**
- [ ] 把 `v001` 标签对齐到 `7dcdd31`（让"v001 = 001 发布点"名副其实）
- [ ] 在 v002 上写 AI 解读功能代码（见第四节清单）

**待你确认**
- 会话前已存在的未提交改动（README.md / shared/levels.js / server/）归 001 还是 002？

---

## 七、常见误解澄清

- ❌ "本地只有一个文件夹，改了不就全改了？"
  ✅ 文件夹是本地副本 + 分支切换呈现不同版本；线上 3001 在另一台机器，不读它。
- ❌ "我改的是源代码，服务器不就拿到了？"
  ✅ 源代码按分支存于 GitHub；服务器只 `git pull main`。推到 `v002` 分支，服务器收不到。
- ❌ "002 依赖 001 的原代码，会不会改坏 001？"
  ✅ v002 从 main **继承**全部 001 代码（不是复制），改动只在 v002 累加；main 的提交不可变，001 永不被破坏。
- ❌ "端口换 5001/5002 会不会碰到旧站点 8080/8081？"
  ✅ 旧站点是独立 Docker 栈，与本 mentor 项目（3001/3002、5001/5002）物理隔离，永不相交。
